pipeline {
    agent any

    stages {
        stage ('Build docker images') {
            steps {
                sh '''
                    docker build -f ci/Dockerfile-playwright -t custom-playwright .
                    #docker build -f ci/Dockerfile-aws-cli -t custom-aws-cli .
                '''
            }
        }
    }
}